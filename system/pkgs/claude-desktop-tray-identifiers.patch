diff --git a/scripts/patches/tray.sh b/scripts/patches/tray.sh
--- a/scripts/patches/tray.sh
+++ b/scripts/patches/tray.sh
@@ -14 +14 @@ patch_tray_menu_handler() {
-	local tray_func tray_var first_const
+	local tray_func tray_func_re tray_var first_const
@@ -16 +16 @@ patch_tray_menu_handler() {
-		'on\("menuBarEnabled",\(\)=>\{\K\w+(?=\(\)\})' "$index_js")
+		'on\("menuBarEnabled",\(\)=>\{\K[$\w]+(?=\(\)\})' "$index_js")
@@ -22,0 +23 @@ patch_tray_menu_handler() {
+	tray_func_re="${tray_func//$/\\$}"
@@ -25 +26 @@ patch_tray_menu_handler() {
-		"\}\);let \K\w+(?==null;(?:async )?function ${tray_func})" \
+		"\}\);let \K[$\w]+(?==null;(?:async )?function ${tray_func_re})" \
@@ -38 +39 @@ patch_tray_menu_handler() {
-		"async function ${tray_func}\(\)\{.*?const \K\w+(?==)" \
+		"async function ${tray_func_re}\(\)\{.*?const \K[$\w]+(?==)" \
@@ -72 +73 @@ patch_tray_menu_handler() {
-			"s/\((\w+\([^)]*\))\s*,\s*${tray_func}\(\)\s*,/(\1,Date.now()-_trayStartTime>3e3\&\&${tray_func}(),/g" \
+			"s/\(([\$[:alnum:]_]+\([^)]*\))\s*,\s*${tray_func_re}\(\)\s*,/(\1,Date.now()-_trayStartTime>3e3\&\&${tray_func}(),/g" \
@@ -101 +102 @@ patch_tray_inplace_update() {
-	local tray_func local_tray_var tray_var_re
+	local tray_func tray_func_re local_tray_var tray_var_re
@@ -104 +105 @@ patch_tray_inplace_update() {
-		'on\("menuBarEnabled",\(\)=>\{\K\w+(?=\(\)\})' "$index_js")
+		'on\("menuBarEnabled",\(\)=>\{\K[$\w]+(?=\(\)\})' "$index_js")
@@ -109,0 +111 @@ patch_tray_inplace_update() {
+	tray_func_re="${tray_func//$/\\$}"
@@ -111 +113 @@ patch_tray_inplace_update() {
-		"\}\);let \K\w+(?==null;(?:async )?function ${tray_func})" \
+		"\}\);let \K[$\w]+(?==null;(?:async )?function ${tray_func_re})" \
@@ -152 +154 @@ patch_tray_inplace_update() {
-		'const \w+\s*=\s*\w+\("menuBarEnabled"\)' "$index_js")
+		'const [$[:alnum:]_]+\s*=\s*[$[:alnum:]_]+\("menuBarEnabled"\)' "$index_js")
@@ -160 +162 @@ patch_tray_inplace_update() {
-		'const \K\w+(?=\s*=\s*\w+\("menuBarEnabled"\))' "$index_js")
+		'const \K[$\w]+(?=\s*=\s*[$\w]+\("menuBarEnabled"\))' "$index_js")
@@ -239 +241 @@ patch_menu_bar_default() {
-		'const \K\w+(?=\s*=\s*\w+\("menuBarEnabled"\))' \
+		'const \K[$\w]+(?=\s*=\s*[$\w]+\("menuBarEnabled"\))' \
