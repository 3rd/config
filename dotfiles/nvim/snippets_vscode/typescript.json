{
  "bzodenv": {
    "prefix": "bzodenv",
    "description": "zod env schema",
    "body": [
      "import { z } from \"zod\";",
      "",
      "const envSchema = z.object({",
      "  $1",
      "});",
      "",
      "export const env = envSchema.parse(process.env);",
      "",
      "export type Env = z.infer<typeof envSchema>;",
      ""
    ]
  },
  "bcontext": {
    "prefix": "bcontext",
    "description": "\"context\"",
    "body": [
      "import { createContext, useContext \\} from \"react\";",
      "",
      "type $1ContextType = {",
      "  $2",
      "\\};",
      "",
      "const $1Context = createContext<$1ContextType>({\\} as $1ContextType);",
      "",
      "export const $1Provider = $1Context.Provider;",
      "",
      "export const use$1 = () => useContext($1Context);",
      ""
    ]
  },
  "basynctry": {
    "prefix": "basynctry",
    "description": "async try-catch wrapper utility",
    "body": [
      "type Result<T, E = Error> = [data: T, error: null] | [data: null, error: E];",
      "",
      "const tryAsync = async <T>(promise: Promise<T>): Promise<Result<T>> => {",
      "  try {",
      "    const data = await promise;",
      "    return [data, null];",
      "  } catch (error) {",
      "    return [null, error instanceof Error ? error : new Error(String(error))];",
      "  }",
      "};",
      "",
      "const [res, err] = await tryAsync(fetch(\"$1\"));",
      "if (err) {",
      "  // err: Error",
      "} else {",
      "  // res: Response",
      "}"
    ]
  }
}
